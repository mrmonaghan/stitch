name: slack-template-md
actions:
  - name: slack-action-1
    type: slack
    config:
      channels:
        - mike-notification-testing
      message: |
        Plain ol' markdown {{ .name }}
  - name: http-test
    type: http
    config:
      method: get
      path: https://fake-url.com/webhook
      headers:
        - name: Content-Type
          value: application/json
        - name: Header1
          value: headerVal1

        

